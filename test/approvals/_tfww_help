tfww - terraform workspace wrapper

Usage:
  tfww COMMAND
  tfww [COMMAND] --help | -h
  tfww --version | -v

Commands:
  update        update latest release
  completions   eval "$(tfww completions)" in .bashrc
  plan          plan with switching terraform workspace
  apply         apply with switching terraform workspace
  switch        switch or create terraform workspace
  show          show state or plan with swtiching terraform workspace
  push          manually push tfstate for migrate from other terraform project.

Options:
  --help, -h
    Show this help

  --version, -v
    Show version number
