tfww - terraform workspace wrapper

Usage:
  tfww COMMAND
  tfww [COMMAND] --help | -h
  tfww --version | -v

Commands:
  update        update latest release
  completions   eval "$(tfww completions)" in .bashrc
  plan          switch terraform workspace and plan
  apply         switch terraform workspace and apply
  switch        switch or create terraform workspace
  show          swtich terraform workspace and show state or plan
  push          tfstate manually push. for migrate non-workspace to workspace.

Options:
  --help, -h
    Show this help

  --version, -v
    Show version number
